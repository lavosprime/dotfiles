[color]
  ui = auto
[alias]
  amend = commit --all --amend --no-edit
  co = checkout
	rekt = checkout -- .
	sl = log --graph --decorate --branches -n 10 --oneline
	st = status -s -b
  sync = pull --rebase
